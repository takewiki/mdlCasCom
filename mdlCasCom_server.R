mdlCasServer::bankList_query_server(input = input,output = output,session = session,dms_token = dms_token)
mdlCasServer::dailyRpt_query_server(input = input,output = output,session = session,dms_token = dms_token)
mdlCasServer::transaction_query_server(input = input,output = output,session = session,dms_token = dms_token)


  


